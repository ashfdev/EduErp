export default function PortalHomePage() {
  return (
    <main className="flex min-h-screen flex-col items-center justify-center p-8">
      <h1 className="text-2xl font-semibold">Student / Guardian Portal</h1>
      <p className="mt-2 text-gray-600">
        PWA — result, attendance, fees, homework, notices. Works offline-tolerant on low-end
        Android browsers.
      </p>
    </main>
  );
}
