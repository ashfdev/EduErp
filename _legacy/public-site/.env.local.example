NEXT_PUBLIC_API_URL=http://localhost:4000
# Public-site is per-institution (PRD: "headless Next.js front-end"). Until
# Phase 4's domain-based tenant routing exists, one deployment = one tenant,
# set explicitly here.
NEXT_PUBLIC_TENANT_ID=
